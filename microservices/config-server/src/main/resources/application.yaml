
spring.application.name: config-server
spring:
  cloud:
    config:
      server:
        git:
          uri: https://github.com/masoud-moghini/config
          skipSslValidation: true
server:
  port: 8080