spring.application.name: config-server
spring:
  cloud:
    config:
      server:
        git:
          uri: https://github.com/masoud-moghini/boilerplate/config-repo
          skipSslValidation: true